{
  "name": "gameserver-portfolio-loginserver",
  "version": "0.1.0",
  "private": true,
  "description": "Node.js account login and one-time game server ticket issuer",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/app.js",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test tests/*.test.cjs"
  },
  "dependencies": {
    "argon2": "^0.41.1",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "js-yaml": "^4.1.1",
    "mysql2": "^3.24.3",
    "redis": "^5.8.2",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^24.7.2",
    "@types/swagger-ui-express": "^4.1.8",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "qs": "6.16.0"
  }
}
